/*
 * SearchResultCard Messages
 *
 * This contains all the text for the SearchResultCard component.
 */
import { defineMessages } from 'react-intl';

const scope = 'app.components.SearchResultCard';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SearchResultCard component !',
  },
});
