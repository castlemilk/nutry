import styled from 'styled-components';

const Wrapper = styled.div`
  width: 100%;
  padding-right: 40px;
  padding-left: 40px;
  font-size: 2.5vmin;
  position: relative;
  display: block;
  z-index: 1;
`;

export default Wrapper;
