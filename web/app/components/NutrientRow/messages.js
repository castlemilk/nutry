/*
 * NutrientRow Messages
 *
 * This contains all the text for the NutrientRow component.
 */
import { defineMessages } from 'react-intl';

const scope = 'app.components.NutrientRow';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the NutrientRow component !',
  },
});
