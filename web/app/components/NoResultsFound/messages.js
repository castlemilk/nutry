/*
 * NoResultsFound Messages
 *
 * This contains all the text for the NoResultsFound component.
 */
import { defineMessages } from 'react-intl';

const scope = 'app.components.NoResultsFound';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'No Results Found',
  },
});
