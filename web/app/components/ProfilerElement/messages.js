/*
 * ProfilerElement Messages
 *
 * This contains all the text for the ProfilerElement component.
 */
import { defineMessages } from 'react-intl';

const scope = 'app.components.ProfilerElement';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ProfilerElement component !',
  },
});
