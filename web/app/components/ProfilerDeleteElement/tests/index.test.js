// import React from 'react';
// import { shallow } from 'enzyme';

// import ProfilerDeleteElement from '../index';

describe('<ProfilerDeleteElement />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
