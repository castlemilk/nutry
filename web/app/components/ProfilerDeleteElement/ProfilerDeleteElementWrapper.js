import styled from 'styled-components';

const ProfilerDeleteElementWrapper = styled.div`
  position: absolute;
  .delete-button-wrapper:hover {
    position: absolute;
    top: -2px;

  }
`;

export default ProfilerDeleteElementWrapper;
