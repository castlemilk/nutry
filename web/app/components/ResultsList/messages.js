/*
 * Results Messages
 *
 * This contains all the text for the Results component.
 */
import { defineMessages } from 'react-intl';

const scope = 'app.components.Results';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Results component !',
  },
});
