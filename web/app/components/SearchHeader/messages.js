/*
 * SearchHeader Messages
 *
 * This contains all the text for the SearchHeader component.
 */
import { defineMessages } from 'react-intl';

const scope = 'app.components.SearchHeader';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SearchHeader component !',
  },
});
