/*
 * ProfilerAddElement Messages
 *
 * This contains all the text for the ProfilerAddElement component.
 */
import { defineMessages } from 'react-intl';

const scope = 'app.components.ProfilerAddElement';

export default defineMessages({
  plus: {
    id: `${scope}.plus`,
    defaultMessage: 'ADD',
  },
});
