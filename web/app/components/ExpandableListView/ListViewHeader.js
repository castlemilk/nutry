import styled from 'styled-components';

const ListViewHeader = styled.div`
  color: white;
  background: #3774a9;

  .headerText {
    margin-left: 5px;
  }

  .headerIcon {
    margin-left: 3px;
  }
`;

export default ListViewHeader;
