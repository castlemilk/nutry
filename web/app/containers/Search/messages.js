/*
 * SearchB Messages
 *
 * This contains all the text for the SearchB component.
 */
import { defineMessages } from 'react-intl';

const scope = 'app.containers.SearchB';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is SearchB container !',
  },
});
