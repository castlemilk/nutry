/*
 * Profiler Messages
 *
 * This contains all the text for the Profiler component.
 */
import { defineMessages } from 'react-intl';

const scope = 'app.containers.Profiler';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is Profiler container !',
  },
});
