/*
 * DetailedCardView Messages
 *
 * This contains all the text for the DetailedCardView component.
 */
import { defineMessages } from 'react-intl';

const scope = 'app.containers.DetailedCardView';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is DetailedCardView container !',
  },
});
