/*
 * FoodProfile Messages
 *
 * This contains all the text for the FoodProfile component.
 */
import { defineMessages } from 'react-intl';

const scope = 'app.containers.FoodProfile';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is FoodProfile container !',
  },
});
