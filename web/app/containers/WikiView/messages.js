/*
 * WikiView Messages
 *
 * This contains all the text for the WikiView component.
 */
import { defineMessages } from 'react-intl';

const scope = 'app.containers.WikiView';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is WikiView container !',
  },
});
