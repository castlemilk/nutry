/*
 * SummaryCardView Messages
 *
 * This contains all the text for the SummaryCardView component.
 */
import { defineMessages } from 'react-intl';

const scope = 'app.containers.SummaryCardView';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is SummaryCardView container !',
  },
});
